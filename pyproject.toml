[project]
name = "abbreviator"
version = "0.1.0"
description = "Medical abbreviation extraction and validation for DOCX documents"
requires-python = ">=3.11,<3.12"
dependencies = [
    "django>=5.2,<5.3",
    "gunicorn; sys_platform != 'win32'",
    "python-docx",
    "regex",
    "requests",
]

[tool.uv]
package = false